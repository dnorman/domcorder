{
  "name": "domcorder-workspace",
  "version": "0.1.0",
  "description": "DomCorder workspace - captures web pages as videos with full fidelity",
  "private": true,
  "type": "module",
  "workspaces": [
    "demo-app",
    "proto-ts",
    "recorder-player"
  ],
  "scripts": {
    "dev": "cd demo-app && bun run dev",
    "build": "cd demo-app && bun run build",
    "test:proto-ts": "cd proto-ts && bun run test",
    "test:proto-rs": "cd proto-rs && cargo test",
    "test:all": "bun run test:proto-ts && bun run test:proto-rs",
    "clean": "rm -rf demo-app/node_modules proto-ts/node_modules node_modules && rm -rf proto-rs/target"
  },
  "keywords": [
    "dom",
    "recording",
    "capture",
    "video",
    "web",
    "browser",
    "serialization"
  ],
  "author": "Domcorder Team",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.0.0"
  },
  "engines": {
    "bun": ">=1.0.0"
  }
}